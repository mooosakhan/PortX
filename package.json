{
  "name": "portx",
  "version": "1.0.0",
  "description": "PortX - A simple Linux port manager",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron . --disable-gpu --no-sandbox",
    "build": "electron-builder"
  },
  "keywords": ["port", "manager", "network"],
  "author": "Moosa Niko",
  "license": "ISC",
  "devDependencies": {
    "electron": "^39.2.6",
    "electron-builder": "^26.0.12"
  }
}
